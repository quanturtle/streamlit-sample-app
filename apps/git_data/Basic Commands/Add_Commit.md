```
# check which files haven't been added/committed
git status

# add file to later commit
git add <file/folder>

# commit files that have been added
git commit <-m:message> <'message to commit'>

# commit files that have been modified and deleted, but not untracked files 
git commit <-a:all>
```